const express = require('express');
const {getPoolConnection} = require("../db/databse");
const router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {

});

module.exports = router;
